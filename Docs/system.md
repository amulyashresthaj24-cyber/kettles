# Design: Flowmast AI — The Execution Ledger for Solo Workers

Generated by `/office-hours` on 2026-04-25  
Branch: `master`  
Repo: `Gstack testing`  
Status: DRAFT  
Mode: Startup  

---

## Problem Statement

Solo workers juggling multiple clients (freelancers, contractors, consultants) cannot trust that their logged time maps to actual work. They switch between client portals, tools, and notifications so frequently that by day's end, they can't answer: "How much time did I actually spend on Client A vs. Client B?"

This creates three cascading problems:
1. **Underbilled hours** — they can't prove time spent, so they either don't bill or undercharge
2. **Misspent effort** — they think they worked 20 hours on their own startup but actually worked 12 (distraction tax invisible)
3. **Broken self-trust** — productivity feels like acting, not real work

The status quo: paper notes + Todoist + Google Calendar + Toggl + Slack. Fragmented. Untrustworthy. Manual.

---

## Demand Evidence

**Primary user:** A remote freelancer with 2+ clients, no internal accountability, juggling tasks across different client portals.

**Observed pain:**
- Starts day with tasks from Client A (day shift) and Client B (night shift)
- Switches between client portals, loses track of which task belongs to whom
- At end of week, cannot answer "how many billable hours for each client?"
- Current workaround: paper notes + manual time entry + distrust of the numbers

**User quote:** *"I look productive, but the time taken is like I'm acting. If there was a solution where I could assign a task and work and track it, it could be perfect."*

**Market signal:** User is actively looking for a solution and would pay for something that solves this (not yet committed, but interested enough to discuss the problem repeatedly).

---

## Status Quo

**Current workflow:**
1. Morning: writes tasks on paper, assigns deadlines manually
2. During work: sets Pomodoro timer on laptop (not linked to tasks)
3. When timer goes off: doesn't remember which task the timer was for
4. End of day: tries to sync paper notes with Todoist; tasks and times don't match
5. End of week: manually extracts hours per client from fragmented logs (or gives up and guesses)

**Cost:** Hours wasted context-switching between tools, plus loss of billable time due to inability to track accurately.

**Consequence if nothing changes:** Continues to underbill, can't see productivity patterns, doesn't trust their own time.

---

## Target User & Narrowest Wedge

**Ideal user:** Remote freelancer with 2+ simultaneous clients, no manager oversight, working across fragmented task management systems (different portals per client).

**Narrowest wedge:** A timer that forces you to pick ONE task, locks in the time against that task, and generates a weekly report showing hours per client.

**What solves the pain:** Task-linked time tracking. Not willpower enforcement ("don't context-switch"). Not task management. Just: **pick a task, timer runs, time locks to the task, report shows what actually happened.**

---

## Constraints

- **Timeline:** Ship MVP in 2-3 weeks
- **Validation:** Test with founder (self) + freelancer friend + 2-3 similar users before scaling
- **Tech:** Next.js monolith (full control, easy iteration)
- **Distribution:** Web app (no mobile initially; browser-based timer is sufficient)
- **Scope:** Minimum viable — task inbox, Pomodoro timer, weekly report. No integrations yet.

---

## Premises

1. **Your freelancer friend is the wedge customer** — multi-client remote worker who can't trust their time allocation
2. **The core pain is trust friction** (can't verify time maps to reality) + manual friction (forgets to log)
3. **The wedge product is task-linked time tracking** — pick a task, timer runs, time locks to task, weekly report shows hours per client
4. **The defensibility is the ledger** — once it becomes the source of truth for income, it's sticky
5. **The market window is now** — remote work is standard, freelancing is rising, existing tools are fragmented
6. **You're the first user** — this solves YOUR problem first; validation comes through your friend + 2-3 others
7. **The product ships as a web app** (not mobile-first)
8. **Initial feature set is narrow** — task inbox + Pomodoro timer + weekly report
9. **You'll validate with 5-10 early users** before scaling to larger markets

---

## Approaches Considered

### Approach A: Monolith First (CHOSEN)
**Next.js full-stack, single deploy, browser-based timer, simple database schema.** Fastest to validation. You own everything. Ship in 2-3 weeks.

**Why chosen:** You need full control to iterate based on early user feedback. Testing with 5-10 people before scaling justifies the monolith approach.

### Approach B: Modular from Day One
**Separate services (tasks, timer, reporting). Microservices architecture.** Future-proof but slower to ship (4-5 weeks).

**Why not chosen:** You don't know yet if the core hypothesis (task-linked time = trust) is correct. Premature architecture.

### Approach C: Outsource the Hard Parts
**Supabase + Trigger.dev + Stripe. Minimal backend code.** Fastest to ship (1-2 weeks) but third-party dependencies.

**Why not chosen:** You want full control for early iteration and testing.

---

## Recommended Approach: Monolith First (Next.js)

### Architecture

**Tech stack:**
- Frontend: Next.js (App Router), React, Tailwind CSS
- Backend: Next.js API routes, Node.js
- Database: PostgreSQL (local dev), or Postgres hosted (e.g., Railway, Render)
- Auth: NextAuth.js (simple session-based, no OAuth required initially)
- Timer: Browser-based (JavaScript) with server-side fallback for persistence

**Database schema (minimal):**
```
users (id, email, password_hash, created_at)
tasks (id, user_id, client_id, title, description, created_at)
clients (id, user_id, name, hourly_rate, created_at)
pomodoros (id, user_id, task_id, duration_minutes, start_time, end_time, status)
weekly_reports (id, user_id, week_start, week_end, generated_at)
  → computed from pomodoros table
```

**Key design decision:** Timer state lives in the database, not just the browser. If the user closes the tab mid-Pomodoro, the next time they open the app, the timer resumes (or marks the session as incomplete). This solves the "did my work get lost?" anxiety.

### Feature Set: MVP

**1. Task Inbox**
- Add task (title, description, client, estimated time)
- Assign task to a client
- Mark task as "to do," "in progress," or "done"
- No external integrations yet — manual entry only

**2. Pomodoro Timer**
- User picks a task from the inbox
- Clicks "Start Focus Session"
- Timer runs for 25 minutes (configurable, but default 25)
- Browser tab title updates to show remaining time (user can see it even when tab is not focused)
- When timer ends: browser notification + sound + redirect to "session complete" screen
- User confirms: "Did you work the full 25 minutes?" (yes/no)
  - If yes: time is locked to the task, marked as "completed"
  - If no: user can adjust the actual time spent (5 min, 10 min, etc.)

**3. Weekly Report**
- Automatically generated every Sunday at midnight
- Shows:
  - Hours spent per client (total + breakdown by task)
  - Total billable hours (based on hourly rate per client)
  - Distraction time (time logged that wasn't assigned to a task)
  - Productivity patterns (when did you work most)
- Exportable as PDF or shareable link
- Historical reports (user can view past weeks)

---

## UI Design: Core Screens

### Screen 1: Task Inbox
```
┌─────────────────────────────────────────┐
│ Flowmast AI                    [User] ▼ │
├─────────────────────────────────────────┤
│                                         │
│ 📋 Today's Tasks                        │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Client A: Email response            │ │
│ │ Est. 25 min  [Start] [Done]         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Client B: Code review               │ │
│ │ Est. 50 min  [Start] [Done]         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ My Startup: Design mockups          │ │
│ │ Est. 90 min  [Start] [Done]         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ [+ Add Task]                            │
│                                         │
├─────────────────────────────────────────┤
│ This Week: 12h 30m logged | $250 earned│
└─────────────────────────────────────────┘
```

**Interaction:**
- User clicks [Start] on a task
- App navigates to Timer screen

---

### Screen 2: Pomodoro Timer (In Progress)
```
┌─────────────────────────────────────────┐
│ Flowmast AI                    [User] ▼ │
├─────────────────────────────────────────┤
│                                         │
│        Client A: Email response         │
│                                         │
│              24:35                      │
│              (large, centered)          │
│                                         │
│          [⏸ Pause] [■ Stop]           │
│                                         │
│ ⓘ Keep this tab open. Browser will     │
│   notify you when the session ends.    │
│                                         │
├─────────────────────────────────────────┤
│ Session started at 2:30 PM              │
│ Estimated time: 25 minutes              │
└─────────────────────────────────────────┘
```

**Interaction:**
- Timer counts down in real-time
- User can pause/resume (for bathroom breaks, etc.)
- If user closes tab, timer continues server-side
- When timer hits 0:00, browser notification fires

---

### Screen 3: Session Complete (Confirmation)
```
┌─────────────────────────────────────────┐
│ Flowmast AI                    [User] ▼ │
├─────────────────────────────────────────┤
│                                         │
│      ✓ Session Complete!               │
│                                         │
│      Client A: Email response          │
│      25 minutes                         │
│                                         │
│  Did you work the full 25 minutes?     │
│                                         │
│    [Yes, log it] [No, adjust time]     │
│                                         │
│ If you adjust, how many minutes did    │
│ you actually work?                     │
│ ┌─────────────────────────┐            │
│ │ [____] minutes          │            │
│ └─────────────────────────┘            │
│                                         │
│    [Log and Continue] [Back to Tasks]  │
│                                         │
└─────────────────────────────────────────┘
```

**Interaction:**
- User confirms time or adjusts it
- Time is locked to the task
- Task moves to "done" or "in progress" (depends on user choice)
- User returns to inbox to pick next task

---

### Screen 4: Weekly Report
```
┌─────────────────────────────────────────┐
│ Flowmast AI                    [User] ▼ │
├─────────────────────────────────────────┤
│                                         │
│ 📊 Weekly Report                        │
│ Week of April 21 - April 27            │
│                                         │
│ ═════════════════════════════════════   │
│ Client A (Billable)                     │
│ 12 hours 30 minutes → $250              │
│ ═════════════════════════════════════   │
│                                         │
│ ═════════════════════════════════════   │
│ Client B (Billable)                     │
│ 8 hours 15 minutes → $165               │
│ ═════════════════════════════════════   │
│                                         │
│ ═════════════════════════════════════   │
│ My Startup (Not Billable)               │
│ 5 hours 45 minutes → $0                 │
│ ═════════════════════════════════════   │
│                                         │
│ Distraction/Untracked: 2 hours 10 min  │
│                                         │
│ Total: 28h 40m | Revenue: $415         │
│                                         │
│ [📥 Export PDF] [📧 Email] [🔗 Share]  │
│                                         │
└─────────────────────────────────────────┘
```

**Interaction:**
- Report updates in real-time as sessions complete
- User can view historical reports (last 4 weeks)
- Can export as PDF or share a read-only link with clients

---

## Success Criteria

1. **You (founder) can use it daily** for 2 weeks without friction
2. **Your freelancer friend uses it** for 1 week and can generate an accurate weekly report
3. **2-3 additional freelancers** test it and report that the report output matches their expectations
4. **Zero false negatives:** If a user completes a session, the time is always recorded (no data loss)
5. **Trust metric:** User says "I believe this number" when looking at the weekly report

---

## Open Questions

1. **Client hourly rates:** Should rates be configurable per client? (Yes, but v2 feature)
2. **Task templates:** Should users save task templates? (No, too complex for MVP)
3. **Notifications:** Email digest of weekly report? (Yes, add in week 2)
4. **Mobile:** Should timer work on mobile? (No, web-only for MVP)
5. **Integrations:** When do we add client portal integrations? (Post-validation, phase 2)

---

## Distribution Plan

**Initial (MVP):**
- Self-hosted instance (you run it locally or on a cheap VPS)
- Share login with your friend + 2-3 testers
- No public signup yet

**After validation (Week 4+):**
- Deploy to Railway or Render (simple, low-cost)
- Create a public landing page (basic HTML, link to signup)
- Share link with your friend to invite others
- Collect emails on waitlist if demand exceeds capacity

**No fancy growth mechanics yet.** Word-of-mouth from testers is your distribution.

---

## CI/CD Pipeline

**Minimal but functional:**
- GitHub Actions: run tests on every PR (if you write them)
- Automatic deploy on `main` branch to Railway/Render
- No manual deployments after initial setup

**Later:** add monitoring, error tracking (Sentry), analytics (Posthog).

---

## Tech Dependencies

**Required:**
- Node.js 18+
- PostgreSQL 14+
- npm/yarn

**Already familiar with?**
- Next.js (App Router)
- React
- Tailwind CSS
- Basic SQL

If any of these are new to you, allocate extra time for learning.

---

## Risks & Mitigations

| Risk | Mitigation |
|------|-----------|
| Timer state lost if user closes tab mid-session | Store timer state in database; resume on app load |
| User forgets to confirm session completion | Auto-confirm after 1 hour idle; send notification reminder |
| Database schema needs changes after launch | Document migration path; v1 has simple schema, easy to evolve |
| Early users churn because feature set is too minimal | Set expectations (beta); collect feedback weekly; iterate fast |

---

## The Assignment

**What you do next (not what you build):**

1. **This week:** Talk to your freelancer friend. Show him this design doc. Ask:
   - "Would you actually use this if it existed?"
   - "What's the one thing you'd change?"
   - Get 1-2 more freelancers interested (peers, community, Twitter)

2. **Week 2:** Set up the dev environment (Next.js, PostgreSQL). Build Task Inbox + basic schema.

3. **Week 3:** Build the Pomodoro Timer. Make sure timer persists to database.

4. **Week 4:** Build the Weekly Report. Generate reports automatically.

5. **Week 5:** Deploy to Railway. Have your friend + 3 testers use it for 1 week. Collect feedback.

6. **Week 6:** Iterate based on feedback. (What did users actually do vs. what you expected?)

**The real assignment:** Before you write a single line of code, **get your friend to commit to trying it for one week.** That's your proof point. Then build.

---

## What I Noticed About How You Think

1. **You identify the real problem, not the surface symptom.** You didn't say "I need a productivity app." You said "I need to trust my numbers." That's founder thinking.

2. **You pushed back on your own assumptions.** When I asked "why wouldn't your friend just use Toggl?", you didn't get defensive. You sat with it and found the real answer: trust friction, not tool friction.

3. **You're building for yourself first.** You said "yes, I would build this even if my friend never paid." That's the only way to build products people want — start with your own pain.

4. **You recognize the ledger as the defensibility, not the features.** You said "if Flowmast becomes a system of record + execution standard, it wins." Most founders would have said "I'll add integrations and mobile." You saw the moat.

---

## Next Steps

- Review this design doc. Do you agree with the premises and approach?
- Talk to your friend. Get commitment (even informal: "yeah, I'll try it").
- Set a start date for week 1 of building.
- Come back with feedback after you've validated with 3-5 testers.

The design is solid. The market signal is there. The only unknown is whether you'll actually ship.

---

**Status: APPROVED** (pending your review)

## Design Specification (from /plan-design-review — 2026-04-25)

### Visual Identity

**Aesthetic direction:** Dark minimal ledger — trust tool for professionals, not a gamified productivity app.

**Typography:**
- Timer numbers: `IBM Plex Mono` (or `JetBrains Mono`), 96px mobile / 128px desktop, weight 300
- UI headings + body: `Plus Jakarta Sans` (or `Inter`)

**Color palette:**
```css
:root {
  --color-bg: #FAFAFA;
  --color-surface: #FFFFFF;
  --color-text-primary: #111111;
  --color-text-secondary: #666666;
  --color-accent: #1A1A1A;        /* primary buttons — black, not blue */
  --color-success: #16A34A;       /* green: session confirmed, trust signal */
  --color-destructive: #DC2626;   /* red: abandon, destructive actions */
  --color-border: #E5E5E5;

  /* Client color-coding (4 muted colors) */
  --client-color-1: #0D9488;      /* teal */
  --client-color-2: #D97706;      /* amber */
  --client-color-3: #BE185D;      /* rose */
  --client-color-4: #4F46E5;      /* indigo */
}
```

**Spacing:** Base 4px unit. 8/16/32px for tight/standard/loose.
**Border radius:** 8px cards, 6px buttons. Never uniform large radius everywhere.
**Icons:** Line icons only (Lucide or Heroicons), 16px in text, 20px standalone.

### Information Architecture

**Navigation:** Top nav with 2 destinations: `[Inbox]` and `[Report]`. No sidebar for MVP.

**Task Inbox hierarchy:**
1. Active session banner (if timer running) — task name, elapsed time, Pause/Stop/View controls
2. Task list grouped by client, sorted: in_progress → todo → done
3. Weekly total footer
4. [+ Add Task] inline expandable row (not a modal, not a new page)

**Timer screen:** Full-page, separate route. Enforces "I am now focused" commitment.

**Session Complete screen:** Reachable after timer hits 0:00 OR user clicks [Stop] early.
- Copy: "Session logged" (not "Session Complete")
- Show exact start/end times: "2:30 PM – 2:55 PM" — trust evidence
- Button copy: `[Yes — log 25 min]` and `[I worked less]` (not "No, adjust")
- Stop early: go directly to Session Complete with actual elapsed time (no confirmation dialog)

**Completion animation:** When timer hits 0:00 — green flash (300ms), brief pause, slide to Session Complete. Not confetti. Not a party. A receipt.

### Interaction States

| Screen | Loading | Empty | Error | Success | Partial |
|--------|---------|-------|-------|---------|---------|
| Task Inbox | Skeleton shimmer (3 cards) | "No tasks today. Pick a task, hit Start, trust the clock." + [+ Add Task] | "Couldn't load tasks. Try refreshing." | Task added: green flash on card | Partial list + error banner |
| Timer | 100ms spinner then countdown | N/A | "Session failed to start. Try again." | Green flash at 0:00 | Paused: timer frozen, pause icon pulsing |
| Session Complete | Logging… spinner after [Yes, log it] | N/A | **Blocking modal**: "Couldn't log your session. [Retry] [Abandon]" — cannot navigate away | "Logged!" with checkmark animation | Adjusted time shown ("18 min logged") |
| Weekly Report | Skeleton bars (3 rows) | "Nothing logged yet this week. Start your first session." | "Couldn't load report. Try refreshing." | N/A (live data) | "Week in progress" label on partial totals |

**Active session banner** (shown on inbox when timer is running in another tab):
- Shows: task name, client badge, elapsed time (polled every 10s via GET /api/pomodoros/active)
- Controls: [Pause] [Stop] [View Timer]
- Prevents accidental second session start

### Responsive Strategy

**Desktop-only MVP.** Show a message on screens < 768px:
```
"Flowmast works best on desktop. Please switch to a larger screen."
```

**Accessibility minimums (non-negotiable even for desktop-only):**
- Button contrast: 4.5:1 minimum
- Visible keyboard focus ring: 2px outline, `#1A1A1A`
- Timer region: `role="timer"` + `aria-live="polite"`
- All form fields: visible labels (not placeholder-as-label)

### Component Build Order

1. `TaskCard` — client color dot + title + time estimate + Start/Done buttons
2. `TimerDisplay` — IBM Plex Mono countdown
3. `ClientBadge` — small color dot + client name label
4. `SessionBanner` — active session indicator with elapsed time
5. `WeeklyBar` — horizontal bar per client in report

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 7 issues, 2 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR (FULL) | score: 3/10 → 8/10, 9 decisions |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CROSS-MODEL:** Outside voice (Claude subagent) challenged BullMQ (reverted to timestamp approach) and hourly_rate (dropped from MVP). Both accepted.
- **UNRESOLVED:** 1 (nav header vs sidebar — deferred to implementation; use top nav)
- **VERDICT:** ENG + DESIGN CLEARED — ready to implement.

